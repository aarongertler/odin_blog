<h1>All Articles Tagged With "<%= @tag.name %>"</h1>

<ul>
  <% @tag.articles.each do |article| %>
    <%= link_to article.title, article_path(article) %>
  <% end %>
</ul>

<%= link_to "Delete tag", tag_path(@tag), method: :delete, data: {confirm: "Are you sure? This will remove the tag from all articles."} %>